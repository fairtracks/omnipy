# CLAUDE.md

Use `AGENTS.md` at the repository root as the canonical instruction file for this repo.

Key points for this repository:
- Follow the exact commands and quirks documented in `AGENTS.md`.
- Prefer the repo's `uv` workflow over ad hoc Python tooling.
- Keep changes minimal and verify with the focused commands from `AGENTS.md`.

If this file and `AGENTS.md` ever differ, trust `AGENTS.md` and update this file to match.
